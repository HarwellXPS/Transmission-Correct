# Transmission-Correct
A small tool to select or batch correct VAMAS files for their in-built Transmission Function Curve. This tools primary purpose is to generate transmission corrected data for plotting, but can also bs used to ‘hard-code’ the stored Transmission Function into the data set
